from setuptools import setup

setup(
    name='chess',
    version='0.0.1',
    install_requires=['gym']  # and other dependencies
)

setup(
    name='draughts',
    version='0.0.1',
    install_requires=['gym']  # and other dependencies
)

setup(
    name='gym_go',
    version='0.0.1',
    install_requires=['gym']  # and other dependencies
)
